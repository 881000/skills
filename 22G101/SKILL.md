---
name: 混凝土结构施工图
description: "混凝土结构施工图查询与解读技能。基于官方图集 PDF 原文：《22G101-1现浇混凝土框架、剪力墙、梁、板》《22G101-2现浇混凝土板式楼梯》《22G101-3独立基础、条形基础、筏形基础、桩基础》《23G101-11 G101系列图集常见问题答疑图解》。当用户询问混凝土结构构件的平法制图规则、配筋构造、钢筋锚固与搭接、梁柱节点、剪力墙、板、楼梯、基础等施工图相关问题，或要求查询图集条文、核对构造做法、解释图集常见问题答疑时使用。回答时提供图集原文（不改写、不删减）、条理清晰的内容摘要，并标注对应的图集号与页码。"
---

> 作者：hxw ｜ 工作室：https://18.cx

# 混凝土结构施工图查询技能

## 概述

本技能以 4 本官方图集 PDF 为唯一依据，回答混凝土结构施工图相关问题。每次回答统一输出三部分：**图集原文**（逐字引用，不改写、不删减）、**内容摘要**（条理清晰、表述合理的要点归纳）、**图集引用**（对应图集号 + 页码，含构造图号）。查询内容全部来自本技能打包的图集，不凭空补充条文。

**高效检索采用「txt 快查 + PDF 出原图」混合方案**：

- `references/*.txt` 是 4 本图集的全文索引（22G101-1/2/3 为排版文字层，23G101-11 为双层 PDF 的隐藏文字层），关键词检索**秒级返回**，无需每次打开大 PDF 逐页扫描（实测约 0.2s vs 2.6s）。
- 命中页码后，用 `assets/` 下的源 PDF **按需渲染目标页原图**，图纸/表格以图为准，不受文字层噪声影响。

## 图集范围

| 图集号 | 全称（主题） | 覆盖内容 | 文件类型 | 文件（assets/） |
|------|------|------|------|------|
| 22G101-1 | 现浇混凝土框架、剪力墙、梁、板 | 柱、剪力墙、梁、板等构件的平法制图规则与构造详图 | 文字版（可检索） | 22G101-1现浇混凝土框架、剪力墙、梁、板.pdf |
| 22G101-2 | 现浇混凝土板式楼梯 | 板式楼梯平法制图规则与构造详图 | 文字版（可检索） | 22G101-2现浇混凝土板式楼梯.pdf |
| 22G101-3 | 独立基础、条形基础、筏形基础、桩基础 | 各类基础平法制图规则与构造详图 | 文字版（可检索） | 22G101-3独立基础、条形基础、筏形基础、桩基础.pdf |
| 23G101-11 | G101系列图集常见问题答疑图解 | 系列图集常见问题答疑与图解（一般构造、柱与节点、剪力墙、梁、板、楼梯、基础等） | 双层PDF（文字层可检索） | 23G101-11 G101系列图集常见问题答疑图解.pdf |

## 工作流程

### 第 1 步：判定问题所属图集

| 问题主题 | 首选图集 | 备选 |
|------|------|------|
| 框架柱、剪力墙、梁、板 | 22G101-1 | 23G101-11 |
| 板式楼梯 | 22G101-2 | 23G101-11 |
| 独立基础、条形基础、筏形基础、桩基础 | 22G101-3 | 23G101-11 |
| 常见问题答疑、多图集综合问题 | 23G101-11 | — |

### 第 2 步：按图集类型检索

**文字版图集（22G101-1 / 22G101-2 / 22G101-3）**

先用 txt 快查定位页码（默认走 `references/*.txt`，缺失自动回退 PDF 扫描）：

```bash
# 在全部图集中检索
python scripts/search_atlas.py --keywords "锚固长度"

# 指定图集、多关键词（默认要求同一页同时命中，多页对照）
python scripts/search_atlas.py --keywords "梁柱节点" "箍筋加密区" --atlas 22G101-1

# 限定返回页数、调整上下文长度
python scripts/search_atlas.py --keywords "搭接长度" --atlas 22G101-3 --limit 5 --context 500
```

脚本返回命中页的 PDF 页码与图集内部页码。涉及**构造详图、数值表格**或需核对原貌时，用源 PDF 渲染对应页原图交付（也用于复核文字层噪声）：

```bash
python scripts/search_atlas.py --render "22G101-1:88" --out-dir ./atlas_out
python scripts/search_atlas.py --render "22G101-3:50-52" --out-dir ./atlas_out
```

**23G101-11（双层 PDF，同样走 txt 快查）**

该图集为「扫描图像 + 隐藏文字层」，文字层可直接检索，与其他分册同样用 `search_atlas.py` 关键词快查：

```bash
python scripts/search_atlas.py --keywords "滑动支座" "抗震构造措施" --atlas 23G101-11
```

注意：其隐藏文字层由识别生成，噪声较大（如「锚固」→「锚因」、竖排侧栏串行），命中后务必 `--render` 目标页原图核对原文。涉及构造图/表格时直接看原图：

```bash
python scripts/search_atlas.py --render "23G101-11:137" --out-dir ./atlas_out
```

文字层噪声导致页码提取不可靠时，用 `--map` 换算兜底：

```bash
python scripts/search_atlas.py --map 23G101-11:7-1   # 内部页码 → PDF 页码
python scripts/search_atlas.py --map 23G101-11       # 列出全部章节首页映射
```

**23G101-11 章节首页映射（内部页码 → PDF 页码）**

| 章节 | 内容 | 内部首页 → PDF 页 |
|------|------|------|
| 1 | 一般构造 | 1-1 → 13 |
| 2 | 柱和节点构造 | 2-1 → 36 |
| 3 | 剪力墙构造 | 3-1 → 55 |
| 4 | 梁构造 | 4-1 → 71 |
| 5 | 板构造 | 5-1 → 97 |
| 6 | 基础构造 | 6-1 → 106 |
| 7 | 楼梯构造 | 7-1 → 137 |
| 8 | 附录 | 8-1 → 139 |

换算规则：内部页「C-P」对应 PDF 页码 = 上表 C 章首页 PDF 页码 +（P − 1）。例如 6-7 → 106 + 6 = PDF 112。

### 第 3 步：按需渲染原图

文字版图集命中页多为「制图规则」文字页；凡涉及构造详图、数值表格，或需核对原貌，用 `--render` 把源 PDF 目标页渲染成高清 PNG，配合文字一起交付（原图忠实于图集，是「以原文、不修改、不删除」的最直接体现）：

```bash
python scripts/search_atlas.py --render "22G101-1:88" --out-dir ./atlas_out
python scripts/search_atlas.py --render "22G101-3:104" --out-dir ./atlas_out
```

### 第 4 步：汇总输出

按下述「输出规范」组织回答：原文引用 → 内容摘要 → 图集引用。

## 输出规范

### 图集原文（不改写、不删减）

- 从脚本返回的原文片段或渲染页中，**逐字引用**与问题直接相关的原条文，放入引用块；不得改写措辞、不得删减关键限定条件。
- 保留原条文中的数值、符号与单位（如 laE、d、φ、@、加密区范围等）。
- 文字版 PDF 文字层含图面噪声（竖向栏、乱码、符号误识）时：正文可读部分按原文引用；明显噪声不引用，并注明「文字层存在识别噪声，以图集原页为准」。
- 原文较长时只引用相关条款，不引用整页无关内容。

### 数值表格（必须完整呈现）

- **完整表格规则**：回答涉及图集数值表（如 la/laE 锚固长度表、ll/llE 搭接长度表、保护层表、楼梯类型表、箍筋加密区表等）时，必须以**渲染原图为数据源，将表格的【全部行 × 全部列】完整转录**：
  - 全部钢筋种类行（HPB300、HRB400/HRBF400/RRB400、HRB500/HRBF500 等，一项不落）；
  - 全部抗震等级档（一、二级 / 三级等）；
  - 全部混凝土强度等级列（C25~≥C60）与直径档（d≤25 / d>25）。
- **禁止只摘局部**：不得只列一个钢筋种类或几个强度等级作为"示例"而省略其余；表格过大时可分多段完整列出，并说明表内单位（如 ×d）。
- 表格数据取自渲染原图视觉读取；文字层噪声以原图为准，必要时逐格核对。

### 内容摘要

- 在原文之后，用「要点」分条归纳，做到条理清晰、表述合理：先讲结论性构造要求，再讲数值、适用条件与例外。
- 每条摘要须能在原文中找到对应依据，并在行尾标注页码（如「——22G101-1 第2-8页」）。
- 摘要中不得出现原文没有的构造要求或数值；确需补充一般工程常识时，须单独注明「（补充说明，非图集原文）」。

### 图集引用

统一格式：**图集号 + 页码 + 图号（如有）**。页码优先用图集内部页码（如 22G101-1 第2-8页）；内部页码不可得时用「PDF 第 X 页」并注明。构造图以「图 X.X.X-X」号引用（如 图4.2.4-4）。

## 表述规则

1. **术语规范**：使用图集与工程规范术语（如「纵向受力钢筋」「箍筋加密区」「绑扎搭接」「机械连接」），不用口语化表述。
2. **逻辑条理**：按「构造位置 → 构造要求 → 数值/条件 → 例外/注意」的顺序组织要点。
3. **区分来源**：明确标注「图集原文」「摘要（依据图集）」「补充说明（非图集原文）」三类内容，避免混淆。
4. **不确定处理**：检索无命中或页面不清晰时，如实说明，改用同义词、图集内部术语重试，或查看目录页定位；不得编造条文。

## 注意事项

- 本技能只查 `assets/` 下的 4 本图集，不引用其他版本（如 16G101）条文。
- 22G101 系列替代 16G101 系列，回答时如涉及新旧差异以本图集为准。
- 图集为官方出版物，本技能用于查询、学习与工程引用，不用于商业传播。

## 资源说明

- `assets/`：4 本图集 PDF（22G101-1、22G101-2、22G101-3、23G101-11），用于渲染原图。
- `references/`：4 本图集的全文索引 `22G101-1.txt`、`22G101-2.txt`、`22G101-3.txt`、`23G101-11.txt`（每页以 `--- PDF页N ---` 分隔），关键词快查用。
- `scripts/search_atlas.py`：图集检索与页码换算脚本（依赖 pymupdf；默认走 txt 快查，缺失自动回退 PDF 扫描；支持检索、--map 页码换算、--render 渲染目录页/任意页）。
- `scripts/extract_kb.py`：从 assets/ 源 PDF 重建 references/*.txt（一次性维护用）。
