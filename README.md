# Agent Skills ｜ 可靠、可控、可复用的 AI 能力库

> 作者：hxw ｜ 工作室：https://18.cx

## 📌 项目简介

**Agent-Skills** 是一套面向 AI Agent 设计的轻量化、标准化、可落地的**通用技能包**。旨在解决 Agent 能力零散、执行不稳定、复用成本高、行为不可控的问题，将零散的工具调用、任务流程、行为规范封装为标准化 Skill 单元。

本仓库聚焦 **可靠性、可控性、可复用性、可扩展性** 四大核心目标，让 AI Agent 告别"随机发挥"，具备稳定、规范、可迭代的执行能力，适配自动化任务、智能助手、业务流程 Agent 等各类场景。

技能包采用"即插即用"的设计理念，每个技能独立封装、统一规范，Agent 可通过读取技能目录下的 `SKILL.md` 自动发现并挂载能力，无需修改核心框架。目前已覆盖**工程建设监理文档**与**创意设计**两大领域，后续将持续扩展更多行业场景。

## ✨ 核心优势

相较于零散的 Agent 能力实现方式，本技能包具备四大核心特性：

- **✅ 可靠（Reliable）**：内置异常捕获、参数校验、重试机制、结果校验，规避调用失败、参数错误、结果不稳定等常见问题，任务执行成功率大幅提升。
- **✅ 可控（Controllable）**：统一行为规范、权限管控、日志溯源、流程约束，所有技能执行可追溯、可拦截、可配置，杜绝越权、乱执行、无效操作。
- **✅ 可复用（Reusable）**：技能模块化、标准化封装，解耦业务逻辑与 Agent 核心逻辑，支持一键引入、跨项目复用、组合拼装复杂任务。
- **✅ 可扩展（Extensible）**：统一的技能注册、挂载、调用规范，新增技能无需改造核心框架，快速迭代适配各类新场景。

## ✅ 可靠性保障机制

为有效解决 Agent 执行不稳定问题，文档生成类技能内置全套保障机制：

- **参数强校验**：自动校验入参类型、长度、合规性，拒绝无效调用
- **异常自动降级**：执行报错自动捕获，返回规范错误信息，避免任务中断
- **重试容错机制**：网络波动、临时报错自动重试，提升任务成功率
- **全链路日志**：记录每一次技能调用的入参、出参、耗时、状态，可溯源排查问题
- **行为可控约束**：限制高危操作、配置权限白名单，避免违规执行

> 创意设计类技能基于 AI 生图工具驱动，可靠性由生图工具与技能工作流中的质量检查环节共同保障。

## 📦 已包含技能

### 🏗️ 工程监理类

| 技能目录 | 名称 | 说明 |
|---------|------|------|
| `supervision-diary` | 监理日志 | 基于标准模板生成格式规范的监理日志 docx 文件，支持四大核心板块内容优化、智能摘要、自动编号与专业排版控制 |
| `supervision-notice` | 监理通知单 | 基于标准模板生成监理通知单 docx 文件，支持事由精简优化、正文专业表达与自动段落编号、整改要求自动追加 |

### 🎨 创意设计类

| 技能目录 | 名称 | 说明 |
|---------|------|------|
| `photo-collage` | 实景拼贴 | 将用户照片做成竖版 3:5 纸刊拼贴海报，保留真实摄影部分，以抽象插画场、高纯度色相结构与手撕纤维边缘重构场景 |
| `image-distillation` | 影像蒸馏 | 将照片蒸馏成独立成立的极简纸刊插画海报，不保留任何原始照片像素，由原创插画、艺术主张、情绪张力与作者性文字构成 |
| `morandi-movie-poster` | 莫兰迪电影海报 | 将个人照片转成源图忠实、纯文字驱动的莫兰迪电影感海报，照片作为锁定底版不变，仅通过标题排版、负空间与选择性遮挡构建电影感效果 |

## 📂 仓库结构

项目采用模块化设计，每个技能独立成目录，结构清晰、易于维护与扩展：

```Plain Text
.
├── supervision-diary/        # 监理日志
│   ├── SKILL.md              # 技能说明文档
│   ├── assets/               # 模板与静态资源
│   └── scripts/              # 技能执行脚本
├── supervision-notice/       # 监理通知单
│   ├── SKILL.md
│   ├── assets/
│   └── scripts/
├── photo-collage/            # 实景拼贴
│   └── SKILL.md
├── image-distillation/       # 影像蒸馏
│   └── SKILL.md
├── morandi-movie-poster/     # 莫兰迪电影海报
│   └── SKILL.md
└── README.md                 # 项目说明文档
```

### 技能目录规范

文档生成类技能遵循统一的三段式结构，便于 Agent 自动发现与调用：

| 目录/文件 | 说明 |
|-----------|------|
| `SKILL.md` | 技能元信息（name、description）与完整使用文档，Agent 据此判断触发时机与调用方式 |
| `assets/` | 模板文件、参考素材等静态资源（文档生成类技能必需） |
| `scripts/` | 技能核心执行脚本，接收命令行参数并输出结果（文档生成类技能必需） |

> 创意设计类技能基于 AI 生图工具驱动，无需 `assets/` 和 `scripts/` 目录，仅通过 `SKILL.md` 中的完整工作流指导 Agent 执行。

## 🚀 快速开始

### 环境要求

- Python 3.8+（文档生成类技能）
- 依赖包：`python-docx`

### 1. 克隆仓库

```Plain Text
git clone https://github.com/881000/skills.git
cd skills
```

### 2. 安装依赖

文档生成类技能统一依赖 `python-docx`：

```Plain Text
pip install python-docx
```

创意设计类技能无需安装额外依赖，由 Agent 调用 AI 生图工具执行。

### 3. 调用技能

#### 文档生成类（以监理日志为例）

```bash
python3 supervision-diary/scripts/generate_diary.py \
  -o 输出文件路径.docx \
  --project "XX工程项目" \
  --date "2026年8月30日" \
  --weather-am "晴" \
  --weather-pm "多云" \
  --temp-high "32" \
  --temp-low "24" \
  --construction "当日施工进展原始描述..." \
  --supervision "当日监理工作原始描述..." \
  --problem "当日问题及处理原始描述..." \
  --other "其他事项..."
```

长文本可写入文件后通过 `--xxx-file` 参数传入：

```bash
python3 supervision-diary/scripts/generate_diary.py \
  -o 输出.docx \
  --project "XX项目" \
  --date "2026年8月30日" \
  --construction-file ./施工内容.txt \
  --supervision-file ./监理内容.txt \
  --problem-file ./问题内容.txt
```

所有文档生成类技能均支持：

- 命令行参数直接传入文本内容
- `--xxx-file` 参数从文件读取长文本
- 输出标准格式 docx 文件，可直接打开编辑

#### 创意设计类（以实景拼贴为例）

在支持技能的 Agent 环境中，直接上传照片并描述需求，例如：

> 用实景拼贴技能把这张照片做成纸刊海报。

Agent 会自动读取对应 `SKILL.md` 中的工作流，完成场景分析、构图决策、prompt 编译与图片生成。

## 🔧 新增技能开发

本技能包支持快速扩展，新增技能遵循以下规范即可被 Agent 自动识别与调用：

1. **新建技能目录**：在仓库根目录下创建 `your-skill-name/` 文件夹（使用英文小写连字符命名）
2. **编写 SKILL.md**：包含 frontmatter（`name`、`description`）和完整使用文档，description 中明确触发关键词和适用场景
3. **放置资源**：文档生成类技能在 `assets/` 中存放模板、参考文件等静态资源
4. **编写脚本**：文档生成类技能在 `scripts/` 中实现核心逻辑，统一使用命令行参数接收输入
5. **遵循规范**：输出格式稳定、参数有默认值、异常有兜底，确保可靠执行
6. **添加作者信息**：在 SKILL.md 的 frontmatter 后、正文标题前添加作者引用块
7. **更新 README**：在「已包含技能」章节对应分类中新增一行，保持清单与实际目录同步

## 📋 路线图

后续将持续扩展更多领域技能包，包括但不限于：

- 工程监理系列：监理月报、旁站记录、验收记录、监理规划等
- 创意设计系列：更多海报风格、插画类型、视觉处理技能
- 通用办公系列：会议纪要、工作报告、合同审查等
- 其他行业领域技能

欢迎提交 Issue 和 Pull Request 参与共建。

## 📄 许可证

本项目采用 **MIT License** 开源协议，可自由使用、修改与分发。使用时请保留原作者信息。

## 📮 联系方式

- 作者：hxw
- 工作室：https://18.cx
- 项目地址：https://github.com/881000/skills
